#!/bin/sh
# run this to generate the node_modules directory for the current platform
# note: for some reason this doesn't seem to work when it's installed globally, not sure why
npm install electron --save-dev
